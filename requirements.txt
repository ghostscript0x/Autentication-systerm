# ============================================================================
# Django Authentication System - Python Dependencies
# ============================================================================
#
# Install with: pip install -r requirements.txt
# ============================================================================

Django>=5.0,<5.1
python-dotenv>=1.0.0
Pillow>=10.0.0

# Production WSGI server
gunicorn>=23.0.0

# PostgreSQL adapter (use psycopg2-binary for dev, psycopg2 for production)
psycopg2-binary>=2.9.0

# Parse DATABASE_URL into Django settings
dj-database-url>=2.0.0

# Serve static files efficiently in production
whitenoise>=6.7.0

# Cloudinary media storage for user uploads (avatars)
cloudinary>=1.40.0
django-cloudinary-storage>=0.3.0
